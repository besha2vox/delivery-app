import styled from 'styled-components';

export const AddsList = styled.ul`
    display: flex;
    flex-direction: column;
    gap: 12px;
`;

export const AddsItem = styled.li`
    border-radius: 40px;
    overflow: hidden;
`;
